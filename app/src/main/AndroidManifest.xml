<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pl.pwsztar.mobilerestaurant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".RestaurantApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MobileRestaurant"
        tools:targetApi="n">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.activity.LoginActivity.LoginActivity" />
        <activity android:name=".views.activity.HomeActivity" />
        <activity android:name=".views.activity.ShoppingCardActivity.ShoppingCardActivity" />
        <activity android:name=".views.activity.CreateOrderActivity.CreateOrderActivity" />
        <activity android:name=".views.activity.RegisterActivity.RegisterActivity" />
        <activity android:name=".views.activity.SettingsActivity" />
        <activity android:name=".views.activity.AboutUsActivity" />
        <activity android:name=".views.activity.ContactActivity" />
    </application>

</manifest>
